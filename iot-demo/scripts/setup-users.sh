#!/bin/bash

# Setup demo users in USER-ACL chaincode
echo "Initializing demo users..."
echo "Note: Users will be created when they first login/register via web UI"
echo "Pre-configured accounts: alice, bob, admin"
